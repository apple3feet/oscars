# This is a first attempt at a Laravel project in GitHub.
# Only the index page is implemented at the moment.
#
# To install:
# - clone repo to your machine
# - composer install
# - php artisan migrate
# - php artisan db:seed

